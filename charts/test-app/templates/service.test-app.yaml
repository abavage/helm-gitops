apiVersion: v1
kind: Service
metadata:
  labels:
    app: test-app
  name: test-app
  namespace: test-app
spec:
  ports:
  - name: 8080-8080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: test-app
  type: ClusterIP

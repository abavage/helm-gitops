{{- with .Values }}
{{ if eq ( .enabled | toString) "true" }}
{{- range .checks }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  {{- if $.Values.suffix }}
  name: {{ .serviceAccount.name }}-{{ $.Values.suffix }}
  {{- else }}
  name: {{ .serviceAccount.name }}
  {{- end }}
  namespace: {{ .namespace.name | quote }}
  labels:
    {{- include "common.labels" $ | nindent 4 }}
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
    argocd.argoproj.io/sync-wave: {{ .syncwave | default 0 | quote }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  {{- if $.Values.suffix }}
  name: {{ .serviceAccount.name }}-{{ .namespace.name }}-{{ $.Values.suffix }}
  {{- else }}
  name: {{ .serviceAccount.name }}-{{ .namespace.name }}
  {{- end }}
subjects:
  - kind: ServiceAccount
    name: {{ .serviceAccount.name | quote }}
    namespace: {{ .namespace.name | quote }}
{{- end }}
{{- end }}
{{- end }}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    helm.sh/hook: post-install
    helm.sh/hook-weight: "2"
  name: infrastructure-app-of-apps
  namespace: openshift-gitops
spec:
  destination:
    namespace: openshift-gitops
    server: https://kubernetes.default.svc
  project: default
  sources:
    - repoURL: {{ .Values.repoURL }}
      chart: app-of-apps
      helm:
        valueFiles:
          - $values/{{ .Values.infrastructureGitPath }}
    - repoURL: {{ .Values.clusterConfigrepoURL }}
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
